# Application dependencies

require:
    - play -> play master-da9bb15
    - pushplay 0.1
    - play -> hazelcast 0.4
    
repositories:
     - localRepo:
         type: local
         artifact: ${application.path}/../../../[module]
         contains:
             - pushplay
