# Application dependencies

require:
    - play
    - pushplay 0.1
    - play -> hazelcast 0.4
    
repositories:
     - localRepo:
         type: local
         artifact: ${application.path}/../../../[module]
         contains:
             - pushplay
